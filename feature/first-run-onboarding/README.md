# 首次使用设置向导

## 状态

`v1.8.8 (100)` 已作为公开 Pre-release 发布；升级后蓝牙恢复候选修复已完成，等待真实环境人工验收。

自动化与构建已覆盖流程状态、持久化和门禁逻辑；签名、公证、最终 ZIP 锁屏启动、公开资产逐字节校验和从 `1.8.7 (68)` 发现候选更新均已通过。真实 RC003、系统权限、MiRemoteV 2ch、BlackHole 2ch、其他回环设备、豆包、Typeless 和其他第三方语音工具仍需按测试手册完成真机验收。

2026-08-11 已补充升级首次启动恢复：Onboarding 已收到 HID 实体按键但 BLE bridge 尚未 Ready 时会只请求一次重连，空 bridge 状态也会实际启动蓝牙连接；真实 Sparkle 升级与 RC003 路径尚待验收。

2026-08-11 已补充旧安装迁移：没有任何向导状态、但旧 Build 或 Sparkle 能证明 App 曾启动过的用户会直接进入主面板；真正全新安装、中途未完成和主动重新运行仍保留完整流程。自动化已通过，真实 Sparkle 升级仍待验收。

2026-08-11 已补充新配对恢复：从系统蓝牙设置返回遥控器页会主动刷新 discovery；多个 HID service 只有部分成功应用电源键保护时，只监听安全 Location，不再让旧或失败 service 阻断当前遥控器。设备卡显示完整正式名称，文件菜单可直接打开日志目录。

2026-08-11 已完成全流程恢复审计：遥控器页回到前台时同时重建 BLE/HID，直接显示生产 HID 错误并提供重新检测；音频页回到前台自动刷新设备；完成页重新确认当前权限、BLE 和音频仍可用。最终重验不依赖无法跨视图重建保存的临时语音/按键标志，避免修复本身造成新的最终页卡死。


2026-08-15 遥控器页补充首次连接说明：先长按 TV 键约 2 秒直到底部白灯闪烁，再同时长按 Home + Menu 进入蓝牙配对；该说明不改变既有 BLE、HID 或实体按键门禁。

2026-08-17 语音工具页增加豆包输入法与微信输入法的分支设置：选择后使用 macOS 输入源 API 自动切换到对应输入法，展示匹配浅色/深色的输入法配置截图；两个分支都要求系统 Fn 动作为“不执行任何操作”，并展示微信 App 清空 Fn 快捷键的截图。真实文字测试仍负责验证第三方输入法最终可用。

2026-08-17 语音工具页完成无滚动布局修复：四个选择卡等高并顶部对齐，设置指引从左栏迁移到右栏；`1020 × 772` 下当前任务、四项设置和底部导航一次完整可见。全流程其他页面同步复查为无内部滚动。

2026-08-17 系统 Fn 第 3 项补充 macOS 键盘设置浅色/深色原图，明确框出“按下 Fn/地球键时 → 不执行任何操作”，并继续显示实时检测状态。Onboarding 选择同时作为日常首选输入法；豆包/微信分支在后续 Fn 按下时按精确 ID 自动准备输入源。

2026-08-17 修复没有实体遥控器的用户无法完成 Onboarding：准备阶段先并列询问“有遥控器 / 当前没有遥控器”，优先实体遥控器；只有没有遥控器才在下一页选择 iPhone App 或网页版。生产状态已确认遥控器连接时自动进入实体流程。三种方式都必须开启蓝牙、输入监控和辅助功能，并由所选来源完成真实连接、普通按键、语音样本、松开停止和文字上屏。

2026-08-18 修复手机分支按需音频被误判为未就绪：iPhone/网页版只在语音开始时建立虚拟音频输出，停止后正常释放，因此音频页和完成页要求 MiRemoteV 2ch 或 BlackHole 2ch 已选择且仍存在，完整可用性继续由下一页真实语音、PCM、停止和文字验证。实体遥控器的持续输出 Ready 门禁保持不变。三种控制方式统一要求蓝牙、输入监控和辅助功能全部开启；iPhone/网页版的本地网络继续在连接页通过真实会话验证。

2026-08-19 修正音频与文字错误通过：Onboarding 只展示并接受 MiRemoteV 2ch 与 BlackHole 2ch，Beosound、扬声器等普通设备不能通过，主设置页仍可保留其他设备；语音页每次真实会话开始清空旧文字，检测到手动键盘输入时不会通过文字门禁，并提供重新测试提示。

2026-08-19 候选修复 Typeless 与实体遥控器识别：选择 Typeless 时同步保存“语音键模拟 Fn 点按”，三种控制方式通过权限页后都会应用；选择其他工具时关闭。实体遥控器普通按键一旦通过生产 HID 设备匹配和报告解析，即使语音 bridge 仍在准备，也会显示已识别并继续后台恢复；后续语音页仍要求真实开始、PCM、停止和文字。26 项 Onboarding、312 项完整 Swift 测试、42 项项目自检、Apple Silicon / Intel Release 构建与浅深色生产页面检查通过，真实 Typeless 和 RC001 / RC003 仍待验收。

2026-08-20 修复 1.9.3 未绑定遥控器的 HID 首报告死锁：真实日志中设备匹配 157 次却没有一次 HID report，确认 discovery 等待首报告后才打开设备的假设在 macOS 26 不成立。候选实现先以非独占模式探测所有安全设备，首份包含已知普通按键的真实报告再绑定实际操作的遥控器，空闲/未知报告不抢占；多遥控器隔离、电源键保护和首按不丢失保持不变，真实 RC001 / RC003 仍待验收。
2026-08-18 候选修复语音测试输入框偶发没有成为系统实际写入目标：首次出现、App 回到前台和点击重试时都会显式重新聚焦，并继续准备用户选择的豆包或微信输入法；占位符改为与正文相同的 15pt 字号和内容起点。生产页面浅色/深色已复验，真实第三方输入法文字提交仍待现场验收。## 为什么开发

用户首次安装后需要自行理解蓝牙、输入监控、辅助功能、遥控器连接、语音输出设备和第三方语音工具，任一环节失败时很难判断原因。设置向导把这些条件组织成一条现场验证的任务流，只有真实状态满足后才能继续。

## 用户功能

- 首次启动自动显示 Typeless 式双栏向导，顶部只显示“准备 › 设置 › 试一下”。
- Onboarding 加入前已经安装并启动过无线麦的用户升级后自动标记为已完成，不重复设置；只有真正全新安装需要完整流程。
- 依次完成语音工具选择、三项系统权限、遥控器连接与实体按键、音频回环设备选择、真实语音文字测试和三个普通按键体验。
- 音频页直接展示当前设备，可选择 MiRemoteV 2ch、BlackHole 2ch 或其他已安装设备，不把某个产品名作为唯一门禁。
- 不提供跳过或稍后继续；关闭 App 后保存当前页，下次启动恢复。
- 完成前不能进入主面板；进入需要硬件的设置阶段后才启动蓝牙和音频运行时。
- “关于”页可重新运行设置向导，并明确说明该操作只重新检查环境，不会清除现有连接、按键映射或其他设置。
- Onboarding 卡住时仍可通过“文件 → 打开日志所在文件夹”取得 `runtime.log`。
- 完成页会再次确认当前权限、遥控器连接和所选音频输出仍可用，避免前面通过后环境变化却仍进入主面板。
- 完成页会再次确认当前权限、所选控制方式连接和所选音频输出仍可用，避免前面通过后环境变化却仍进入主面板。
- 尚未绑定的实体遥控器会先进入安全的非独占 HID 探测，首个真实按键选择实际操作的设备，不会因等待首报告而永久卡在遥控器页。
## 范围与非目标

本功能复用现有权限、蓝牙、HID、音频设备和语音输出状态，不修改协议、PCM 格式、设备配对方式或按键默认含义。首版不包含 iPhone、网页遥控器或高级映射编辑，它们继续保留在主面板。

## 隐私与兼容边界

- 新增的样本计数只记录当前语音会话收到多少 PCM 样本，不保存样本内容。
- 向导输入框中的测试文字只存在于当前视图状态，不写入设置或业务日志。
- 流程版本和当前位置保存在本机 `UserDefaults`。
- 旧安装迁移只读取既有 Build 记录和 Sparkle 的“曾启动”标记，并保存一次迁移版本；不上传安装历史。
- 隐藏截图入口使用随机独立 `UserDefaults` suite，不读取或修改用户正式向导进度。
- 支持 macOS 14 及以上；中文最终显示字号不低于 12pt。

## 本次方法总结

- 设计目标不是介绍功能，而是把“权限、硬件、音频和第三方语音工具是否真的可用”变成一条不可跳过、可恢复、可诊断的任务流。
- 页面节奏参考 Typeless 的连续截图和双栏信息层级，但不复制其视觉；顶部只保留“准备 › 设置 › 试一下”，不显示步骤数量、百分比或强制措辞。
- 开发时把流程状态、运行时启动门禁和生产能力检测分离，复用现有权限、蓝牙、HID 与音频状态，避免为向导建立第二套业务链路。
- 测试分为状态机自动化、真实生产视图截图、构建发布验证和真实环境人工验收；任何一层都不能替代下一层。
- 锁屏截图问题最终通过 App 内隐藏的离屏 AppKit 渲染入口解决；浅色和深色必须分别固定整个窗口外观，不能只给单侧视图设置颜色方案。

## 相关资料

- [开发记录](development.md)
- [功能测试说明](testing.md)
- [完整人工测试手册](../../Testing/FirstRunOnboarding.md)
- 本机可复用 Skill：`~/.codex/skills/remote-mic-onboarding-screenshots/`
- 详细设计与确认稿保存在私有产品资料库 `projects/remote-mic-app/research/mac-settings-page-redesign/v1/`。
